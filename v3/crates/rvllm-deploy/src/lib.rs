// rvllm-deploy — scaffold only. See v3/specs/16-deploy.md.
//   pub mod tarball;            // pack artifacts + manifest.json
//   pub mod spawn;              // tools/spawn-h100.sh wrapper: vast/runpod/aws
//   pub mod deploy_and_bench;   // end-to-end: verify manifest, deploy, bench-gate
